<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <TextView
        android:id="@+id/gameName_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Game Name"
        android:textColor="@android:color/black"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/gamePrice_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/gameName_textView"
        android:layout_marginTop="10dp"
        android:text="Price : 130"
        android:textColor="@android:color/black" />

    <TextView
        android:id="@+id/gameQuantity_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/gamePrice_textView"
        android:layout_marginTop="10dp"
        android:text="Quantity : 50"
        android:textColor="@android:color/black" />

    <ImageView
        android:id="@+id/deleteImage"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/gameName_textView"
        android:src="@drawable/ic_delete" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_below="@+id/gameQuantity_textView"
        android:layout_marginTop="16dp"
        android:background="#aaa" />

</RelativeLayout>